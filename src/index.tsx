import { render } from "react-dom";
import "./index.css";
import AppContainer from "./presentation/AppContainer";

const rootElement = document.getElementById("root");
render(<AppContainer />, rootElement);
